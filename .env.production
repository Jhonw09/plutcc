# Production — VITE_API_URL must be set in the Vercel dashboard (Settings > Environment Variables).
# The value here is ignored by Vercel — it only applies to local `npm run build`.
VITE_API_URL=
VITE_USE_API=true
